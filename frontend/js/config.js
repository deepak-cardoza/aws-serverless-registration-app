// API Configuration
// Update this with your API Gateway endpoint after deployment
const API_CONFIG = {
    BASE_URL: 'http://localhost:3000', // Change to your API Gateway URL
    ENDPOINTS: {
        REGISTER: '/register',
        LOGIN: '/login'
    }
};
